version: '3.8'

services:
  api:
    build: .
    ports:
      - "8120:8120"
    environment:
      - PORT=8120
      - WORKERS=4
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
    volumes:
      - ~/.aws:/root/.aws:ro
    restart: unless-stopped